# Private — used by Next.js rewrites (server-side only, NOT exposed to browser)
API_URL=http://localhost:4000

# Public — only the socket URL needs to be accessible client-side
NEXT_PUBLIC_SOCKET_URL=http://localhost:4000
