{
  "framework": "nextjs",
  "buildCommand": "pnpm --filter @veda/shared build && pnpm --filter @veda/web build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "pnpm install --no-frozen-lockfile"
}
